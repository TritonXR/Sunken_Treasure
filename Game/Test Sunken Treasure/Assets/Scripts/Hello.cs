using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hello : MonoBehaviour
{
    public bool lmao = true;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void Test() {
        Debug.Log("YO");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
